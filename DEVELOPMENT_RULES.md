# DEVELOPMENT RULES

## 基本方針

このプロジェクトは個人利用専用です。

最優先事項は「毎日安心して使えること」です。

新機能よりも、壊さない、読みやすい、保守しやすい、原因を追いやすいことを優先してください。

## 必須ルール

1. 既存機能を勝手に変更しない
2. 既存ファイルを確認せず削除しない
3. app.pyに業務ロジックを書かない
4. DB処理、株価取得、テクニカル指標、スコア計算、表示部品、入力検証を分離する
5. 重複実装を禁止する
6. 閾値や固定値はconstants.pyまたは設定へ集約する
7. 可能な範囲ですべての関数へ型ヒントを付ける
8. 関数、クラス、モジュールに簡潔なdocstringを書く
9. コメントは処理内容ではなく理由を書く
10. `except: pass`は禁止。必ずログと適切なユーザー表示を行う
11. データ不足を正常系として扱う
12. 画面側でスコア計算しない
13. 画面側で指標計算しない
14. DBアクセスを画面へ直接書かない。DB処理はservices/database.pyを経由する
15. Phase 5まではAI APIを追加しない
16. 仕様外の機能はIDEAS.mdへ記載し、指示があるまで実装しない
17. 変更後にPython構文チェック、importチェック、pytest、Streamlit起動確認、DB初期化確認を行う
18. 推測で修正済みと言わない
19. 仕様変更時はREADME.md、CHANGELOG.md、ROADMAP.md、docs/、TECH_DEBT.mdを同期する
20. 1回の変更を小さくする

## DB変更ルール

21. DBスキーマを破壊的に変更しない

- 既存カラムを勝手に削除しない
- 既存カラムの型を直接変更しない
- 既存ユーザーデータを失う変更を行わない

22. DB変更前にバックアップ方法を示す

DB構造を変更する場合は、`data/orekabu.db` のバックアップ方法をREADME.mdへ記載すること。

23. DB変更時は移行処理を用意する

新しいテーブルやカラムを追加する場合は、既存DBを削除しなくても更新できるようにすること。

24. DBバージョンを管理する

必要に応じて `schema_version` または同等の仕組みを使用し、現在のDB構造を判別できるようにすること。

25. マイグレーションは冪等にする

同じ移行処理を複数回実行しても、エラーや重複データが発生しないようにすること。

## 依存関係ルール

26. ライブラリを勝手に増やさない

新しい依存ライブラリを追加する場合は、標準ライブラリや既存依存で代替できないか確認すること。

27. requirements.txtを同期する

importを追加・削除した場合は、requirements.txtとREADME.mdの必要環境を確認すること。

## テストルール

28. バグ修正には再発防止テストを追加する

可能な限り、修正した不具合を再現するテストを追加すること。

29. 外部通信に依存するテストを分離する

yfinanceなどの外部通信テストは、通常の単体テストと分けて扱うこと。

30. テストを通すためだけに仕様を変えない

テスト失敗時は原因を確認し、正しい仕様に合わせてコードまたはテストを修正すること。

## 外部取得ルール

31. 外部取得データで手動データを自動上書きしない

32. 外部取得処理はプロバイダー層へ隔離する

33. 取得元、取得日時、信頼度を候補データへ保持する

34. 外部通信テストはモックし、通常pytestをネットワークへ依存させない

35. 取得失敗と空データを正常系として扱う

36. 高頻度アクセスを避け、取得上限、間隔、キャッシュを設ける

37. 利用規約、アクセス制限、認証、CAPTCHAを回避しない

38. 候補承認と正式データ更新は同一トランザクションで行う

## UI・自動化ルール

39. UI変更時はStreamlit AppTestまたは必要に応じてE2Eテストを追加する

40. 自動テストで実DBを使用せず、環境変数またはDIで一時DBへ切り替える

41. 外部通信テストを通常テストへ混ぜず、integrationマーカーで分離する

42. 画面確認は可能な限りAppTest、サービス統合テスト、E2Eへ置き換える

## 開示データ・ファイル安全ルール

43. 外部URLはHTTP(S)だけを許可し、file URL、認証情報付きURL、localhost、非公開IPを拒否する

44. ローカルファイルは設定された保存ディレクトリ配下のPDFだけを許可し、拡張子、PDF署名、サイズ、解決後パスを検証する

45. PDF本文やファイル本体をSQLiteへ保存しない

46. CSV出力では数式として解釈される先頭文字を無害化する

47. TDnet等のサイトを自動巡回する場合は、別フェーズで規約・頻度・取得元を確認してから実装する

## 命名規則

Python変数、関数: snake_case

クラス: PascalCase

定数: UPPER_CASE

DB列: snake_case

ファイル名: snake_case

## UIルール

- ダークテーマ
- 情報量は多めだが読みやすくする
- 派手なアニメーションは禁止
- 操作方法を迷わせない
- 色だけで状態を伝えない
- スマートフォンでも大崩れさせない

## Gitルール

コミット可能な環境の場合は1コミット1目的を意識すること。

接頭辞: `feat:` `fix:` `refactor:` `docs:` `test:` `chore:`
