# Technical Debt

確認日: 2026-07-16

## High

現時点でv0.4.0正式版を妨げる重大な技術的負債は確認されていません。

確認した範囲:

- app.pyとpages配下
- services配下
- components配下
- tests配下
- README、ROADMAP、CHANGELOG、DEVELOPMENT_RULES、PROJECT_PRINCIPLES
- Phase 2Aの実画面操作、CSV、日本語版Excel表示、再起動後のデータ保持
- Phase 2Bの候補取得、差分、承認、確定保護、履歴、候補CSV、再起動後保持
- 通常テスト、AppTest、E2E、yfinance実通信、スマートフォン実機表示

## Medium

- タグを複数付与するため、タグ別評価額や損益の単純合計はポートフォリオ合計と一致しない
- タグルールの優先度は利用者が管理し、同順位で内容が異なる場合は自動解決せず個別上書きが必要
- `percent_from_current_price`は表示時点の現在値を基準に再計算されるため、固定された注文価格ではない
- タグCSVはタグ名だけを持つため、同名タグを複数グループに作成した場合は曖昧行として取込を拒否する
- タグ別集計は登録銘柄数の増加時にDB集約へ移す余地がある
- Streamlitのサーバー側では実viewportを確実に判定できないため、カード優先とモバイル優先設定を併用する
- 共通CSSはStreamlitの`data-testid`へ一部依存し、Streamlit更新時に描画確認が必要
- EDINET API v2はAPIキーと外部サービス稼働に依存し、通常テストでは実通信を行わない
- EDINET取得は日次・月次・初回バックフィルの遡及日数を設定できるが、タスクスケジューラ自体の実行漏れ検知は未実装
- 企業情報候補は承認UIを実装済みだが、取得元の英語表記を日本語へ正規化する企業マスターは持たない
- 項目別承認は最新の現在値と候補値を比較して行うが、複数候補間の自動優先順位付けは未実装
- Windowsタスクスケジューラへの登録、資格情報、スリープ復帰は利用者のPC設定に依存する
- RSS、yfinance、EDINETの利用制限値は一律ではないため、日次1回程度の低頻度運用を前提とする
- 自動化履歴とEDINETメタデータの保存期間・アーカイブは未実装
- EDINET実通信integrationテストはAPIキーと外部サービス稼働に依存し、未設定環境ではskipする
- マーケットスピードCSVのインポート履歴は専用テーブルを持たず、実行件数をログへ記録する
- 「既存銘柄をスキップ」と「新規のみ追加」は現在の単一ポートフォリオCSVでは同じ安全動作になる
- マーケットスピードの列名や出力仕様が変更された場合は専用パーサーの追随が必要
- EDINET月次確認は専用BATを持たず、タスクスケジューラから`fetch_edinet.py --preset monthly`を呼ぶ運用
- タスクスケジューラの実行漏れを自動補完しないため、月次確認と必要に応じたバックフィルを併用する

- 企業の市場、業種、略称は手動管理で、自動マスタ連携や表記統一を行わない
- 企業カルテの株価取得は画面表示時に既存yfinance処理を呼ぶため、登録銘柄の切替頻度によって待ち時間が発生する
- 企業カルテのタイムラインは保存済みメタデータを横断取得するため、情報量が増えた場合は期間絞り込みやページングが必要
- テーマは手動のカンマ区切り文字列であり、テーマ辞書や銘柄横断集計は未実装
- 統合タイムラインは文字列のISO日時を基準にし、時刻がない決算日は日単位で並ぶ
- 投資ルールのトレーリングストップは高値基準値を保存していないため、設定値の記録だけで自動到達判定を行わない
- 利確・損切りの接近判定は現在値から一律5%で、銘柄ごとの値動きやボラティリティは考慮しない

- 適時開示は手動・CSV登録のみで、TDnetの変更や訂正を自動検知しない
- PDF本文・XBRLを解析しないため、要約と数値は利用者が一次資料を確認して入力する必要がある
- ニュース関連候補は厳密なURLまたはタイトル包含だけで、表記揺れには対応していない
- ローカルPDFはアプリ削除時に自動削除しないため、`data/disclosures`を利用者が管理する必要がある

- Streamlitはサーバー側で実viewportを判定しないため、カード切替は「モバイル優先表示」設定で明示的に行う。端末幅に応じた自動切替は限定的なCSS調整のみ。
- 今日やることは現在状態から毎回生成し、完了チェックや日単位の履歴は保存しない。

- RSS/Atomは配信元ごとの差異が大きく、標準的な要素以外を使うフィードにはプロバイダー調整が必要になる。
- ニュース本文は保存しないため、RSS要約が短いソースではローカル検索・分析材料が限定される。
- RSSはCLIとWindowsタスクスケジューラから実行可能だが、HTTPキャッシュ（ETag/Last-Modified）は未実装。
- 1回の取得は1ソース5件に固定しており、件数の設定変更は未実装。
- 銘柄照合は単純な文字列一致で、短い会社名・一般語の誤一致や表記揺れに対する高度な正規化は未実装。
- 記事・取得履歴が増えた場合の保存期間設定、アーカイブ、ページングは未実装。

- yfinanceの取得結果は外部サービス状態に依存するため、将来的にはローカルキャッシュ永続化を検討する。
- yfinanceだけでは全企業の将来決算予定日を提供できない。登録済み公式IRへのフォールバックを追加したが、画像のみ、JavaScript描画、具体日未公表のページは自動抽出できない。ETFは企業決算の対象外として候補日を持たない。
- SQLiteマイグレーションはschema_version方式を導入済み。今後の変更でもversion別の移行処理と冪等性テストを継続する必要がある。
- Streamlitキャッシュは時間バケットで制御している。銘柄数が増えた場合の取得頻度やキャッシュ永続化は追加検討が必要。
- 設定ページは1ファイル内のフォーム項目が多く、将来的には設定カテゴリごとに部品化すると保守しやすい。
- 外部通信を伴うyfinance実取得確認は通常pytestから分離している。自動CIがある場合はネットワークテストを別扱いにする必要がある。
- 決算候補は承認制のため、変更や延期の正式反映は利用者の確認が必要。
- schema_versionによるSQLite移行を導入したが、ロールバック用マイグレーションはない。更新前バックアップが必須。
- 関連銘柄は有向関係として保持する。双方向関係は2件登録が必要で、関係の解釈は利用者に委ねられる。
- 過去決算履歴が増えた場合のアーカイブやページングは未実装。
- 候補承認方式で手動データの自動上書きを防いでいる。将来の取得元追加でも同じ優先順位と承認ルールを維持する必要がある。
- yfinanceのcalendarおよびearnings_datesの返却形式は外部仕様変更の影響を受ける。変更時はプロバイダー内の修正が必要。
- yfinance候補の信頼度は低として扱う。yfinance成功時に公式IRと自動照合する処理は行わない。
- 取得元はyfinance、登録済み企業公式IR、CSV。公式IRの会社別専用パーサーは未導入で、追加時も同じ候補契約と監査情報を維持する必要がある。
- 候補と取得履歴は増加する。確認済み候補は保存期間で整理できるが、履歴の自動アーカイブは未実装。
- キャッシュと1秒以上の取得間隔はローカルプロセス単位で、複数プロセス間の排他はDBロックではない。
- yfinance側のレート制限値は公開保証されないため、最大20件の初期値でも取得失敗する可能性がある。
- 発表時刻とタイムゾーンは取得できない場合が多く、時刻は未定として扱う。
- yfinanceから四半期を安定取得できないため、未設定候補が発生する。承認時に人間確認が必要。
- 公式IRページのrobots.txt、HTML構造、URL変更は企業ごとに異なる。取得失敗は正常系として扱い、URLと抽出結果を利用者が定期確認する必要がある。

## Low

- 企業カルテへの遷移はtickerクエリを使用するが、ニュース候補が未承認の場合はカルテのニュース一覧に表示しない
- モバイルレイアウトは設定による手動切替で、ブラウザ幅の自動検出ではない

- 適時開示一覧はカード中心で、件数増加時の全文検索・高度な絞り込みは未実装

- ブリーフィングの対象ページリンクはページ単位で、記事IDや決算候補IDへのアンカー遷移はStreamlit標準構成では未実装。

- ニュース一覧はStreamlit標準テーブル中心で、記事カード、全文検索、複合フィルタは今後の改善候補。

- スマートフォンでの表表示は横スクロール前提。重要列の固定表示は今後の改善候補。
- スクリーンショットは未追加。
- 一部の表示ラベルはさらに短くできる余地がある。
- `python -m compileall .` は `.venv` も走査するため出力が大きい。日常確認では対象ディレクトリを絞る運用も検討する。
- Streamlit 1.59.1で `use_container_width` の将来非推奨警告が出る。2025-12-31以降に備えて `width='stretch'` への置換を検討する。
- 決算カレンダーは外部ライブラリを使わない月別グループ一覧で、一般的な月間グリッド表示ではない。
- 日本時間は標準ライブラリの `Asia/Tokyo` を使用するが、SQLiteの日付比較もサービスから日本日付を渡す前提。
- yfinance候補との日付変更検知は実装済みだが、公式情報による変更検知は未実装。
- AppTestではフォーム、再描画、CSVアップロードを確認したが、ブラウザ固有の描画差やJavaScriptコンソールは検証できない。
- Playwrightは未導入。スマートフォン実機表示は確認済みだが、Desktop・Tablet・Mobileのスクリーンショット差分検知は自動化されていない。
- 日本語版Excel表示は実画面確認済みだが、Excelアプリの将来バージョンに対する自動互換性検証はできない。
