# モバイル表示

## 方針

狭い画面では重要情報を先にし、metricは最大2列、操作ボタンは十分な高さと横幅を確保します。長いタイトルとURLは折り返します。

## モバイル優先表示

設定を有効にすると、ダッシュボード、保有株、監視銘柄、決算、ニュースの主要な横長表を展開式カードへ切り替えます。Streamlitだけではサーバー側のviewportを確実に判定できないため、表示方式は利用者が明示的に選択します。

カードでも状態ラベルを文字で表示し、色だけに依存しません。詳細情報は展開して確認できます。
